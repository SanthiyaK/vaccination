<!DOCTYPE html>
<html>
<head>
  <title>Dynamic Todo List</title>
  <style>
    body { font-family: Arial; padding: 20px; }
    ul { list-style: none; padding: 0; }
    li { padding: 8px; background: #f2f2f2; margin: 5px 0; 
         display: flex; justify-content: space-between; border-radius: 5px; }
    button { background: red; color: white; border: none; padding: 5px; border-radius: 4px; cursor: pointer; }
  </style>
</head>
<body>
  <h2>📝 My Todo List</h2>
  <input type="text" id="taskInput" placeholder="Enter a task" />
  <button onclick="addTask()">Add</button>
  <ul id="taskList"></ul>
  <script>
    function addTask() {
      let input = document.getElementById("taskInput");
      let taskText = input.value.trim();

      if (taskText === "") {
        alert("Please enter a task!");
        return;
      }
      // Create list item
      let li = document.createElement("li");
      li.innerText = taskText;
      // Create remove button
      let removeBtn = document.createElement("button");
      removeBtn.innerText = "X";
      removeBtn.onclick = function() {
        li.remove();
      };  
      // Append button to li
      li.appendChild(removeBtn);
      // Add li to ul
      document.getElementById("taskList").appendChild(li);

      // Clear input
      input.value = "";
    }
  </script>
</body>
</html>
 

<!--  <!DOCTYPE html>
<html>
<body>

<p id="p1">Visible Text</p>
<p id="p2" style="display:none;">Hidden Text</p>

<script>
  let visible = document.getElementById("p1");
  let hidden = document.getElementById("p2");

  console.log("innerText visible:", visible.innerText);     // Visible Text
  console.log("textContent visible:", visible.textContent); // Visible Text

  console.log("innerText hidden:", hidden.innerText);       // "" (empty, because CSS hides it)
  console.log("textContent hidden:", hidden.textContent);   // Hidden Text (still shows text!)
</script>

</body>
</html>

 -->